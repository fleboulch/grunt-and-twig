# Grunt
```bash
# Get all the available taks
grunt -h
  
# Run the livereload php server and other tasks (copy, compass, ...)
grunt  
  
# Deploy the app in prod
bower install # download bower_components
grunt build --env=prod
```
